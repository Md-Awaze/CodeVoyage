# Database Configuration (PostgreSQL)
# For Render PostgreSQL, these will be automatically set by Render
# For local development, use your local PostgreSQL credentials
PGHOST=localhost
PGPORT=5432
PGDATABASE=contact_form
PGUSER=postgres
PGPASSWORD=mysecretpassword

# Application Configuration
NODE_ENV=development
PORT=3000
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Email Configuration (optional - for contact form notifications)
EMAIL_USER=awazemohommed@gmail.com
EMAIL_APP_PASSWORD=your-gmail-app-password

# Base URL for the application (set to your production URL when deploying)
BASE_URL=http://localhost:3000 